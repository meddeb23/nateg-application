# nateg-application

## Run the server locally

- create a .env file with :
  - NODE_env=developement
  - PORT = 5000
  - debug=app:startup,app:routes
  - JWT_SECRET=mySecret
- npm install
- npm run server
